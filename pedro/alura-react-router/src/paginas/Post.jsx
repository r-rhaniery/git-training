import React from 'react'

const Post = () => {
    
    const [post, setPost] = useState({})
    return (
        useEffect(() => {
            busca('/posts/${id', setPost)
            
            return () => {
                cleanup
            }
        }, [input])
        )
}